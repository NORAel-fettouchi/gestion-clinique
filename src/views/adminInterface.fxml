<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.*?>
<?import javafx.scene.text.*?>

<BorderPane stylesheets="@../design/AdminDashboard.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="controllers.AdminDashboardController">

    <!-- Sidebar -->
    <left>
        <VBox fx:id="sidebar" alignment="TOP_CENTER" prefWidth="220" spacing="10" styleClass="sidebar">
            <padding>
                <Insets bottom="20" left="10" right="10" top="30" />
            </padding>

            <Label styleClass="logo" text="Bonjour" />

            <StackPane alignment="CENTER">
                <Circle radius="30" styleClass="user-icon" />
                <Text fx:id="textInitiale" styleClass="user-initial" />
            </StackPane>

            <Label fx:id="labelNomComplet" styleClass="user-info" />
            <Label fx:id="labelId" styleClass="user-info" />

            <Separator />

            <Button fx:id="btnDashboard" maxWidth="Infinity" onAction="#handleDashboard" styleClass="feature-item" text="🏠 Tableau de Bord" />
            <Button fx:id="btnPatients" maxWidth="Infinity" onAction="#handlePatientsButtonAction" styleClass="feature-item" text="👥 Patients" />
            <Button fx:id="btnTraitements" maxWidth="Infinity" onAction="#handleTraitements" styleClass="feature-item" text="💊 Traitements" />
            <Button fx:id="btnRendezVous" maxWidth="Infinity" onAction="#handleRDVButtonAction" styleClass="feature-item" text="📅 Rendez-vous" />
            <Button fx:id="btnStats" maxWidth="Infinity" onAction="#handleStatistiques" styleClass="feature-item" text="📈Statistiques" />
            <Button fx:id="btnDemandes" maxWidth="Infinity" onAction="#handleDemandesButtonAction" styleClass="feature-item" text="📩 Demandes reçues" />
          </VBox>
    </left>

    <!-- Top bar -->
    <top>
        <HBox alignment="CENTER_RIGHT" spacing="10" styleClass="top-bar">
            <padding>
                <Insets bottom="15" left="15" right="15" top="15" />
            </padding>
            <Label text="Bonjour, Admin" />
            <Label text="● En ligne" textFill="green" />
            <StackPane alignment="CENTER">
                <Circle radius="12" styleClass="user-icon" />
                <Text fx:id="textTopbar" styleClass="user-initial1" />
            </StackPane>

            <Button fx:id="btnLogout" onAction="#handleLogout" styleClass="logout-btn" text="Se déconnecter" />
        </HBox>
    </top>

    <!-- Center content -->
    <center>
        <VBox fx:id="mainContent" spacing="20" styleClass="main-content">
            <Label styleClass="page-title" text="Tableau de bord" />

            <HBox spacing="20" styleClass="dashboard-stats">
                <VBox styleClass="stat-card">
                    <Label styleClass="stat-title" text="Patients" />
                    <Label fx:id="lblTotalPatients" styleClass="stat-value" text="0" />
                </VBox>
                <VBox styleClass="stat-card">
                    <Label styleClass="stat-title" text="Rendez-vous" />
                    <Label fx:id="lblTotalRdv" styleClass="stat-value" text="0" />
                </VBox>
            </HBox>

            <TableView fx:id="tableRdv" styleClass="table-view" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colDate" text="Date" prefWidth="340"/>
                    <TableColumn fx:id="colStatut" text="Statut" prefWidth="340"/>
                    <TableColumn fx:id="colView" text="Voir détaille" prefWidth="340" minWidth="340" maxWidth="250">


                    </TableColumn>
                </columns>
            </TableView>

        </VBox>
    </center>


</BorderPane>
